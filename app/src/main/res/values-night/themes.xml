<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DrinksAhoy" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/davys_grey</item>
        <item name="colorPrimaryVariant">@color/sonic_silver</item>
        <item name="colorOnPrimary">@color/clustered</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/warm_black</item>
        <item name="colorSecondaryVariant">@color/liliac_luster</item>
        <item name="colorOnSecondary">@color/clustered</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>
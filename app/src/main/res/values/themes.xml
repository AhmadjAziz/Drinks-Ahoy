<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DrinksAhoy" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:textColor">@color/clustered</item>
        <item name="android:windowBackground">@color/clustered</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/emerald</item>
        <item name="colorPrimaryVariant">@color/emerald</item>
        <item name="colorOnPrimary">@color/clustered</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/fern</item>
        <item name="colorSecondaryVariant">@color/fern</item>
        <item name="colorOnSecondary">@color/clustered</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.DrinksAhoy.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DrinksAhoy.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.DrinksAhoy.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    //Changes the style of the cardView holder to make the edges rounded and give elevation
    <style name="ShapeAppearanceOverlay.MaterialCardView.Cut" parent="Theme.DrinksAhoy">

        <item name="elevation"> 5dp</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">8dp</item>
        <item name="cornerSizeTopLeft">8dp</item>
        <item name="cornerSizeBottomRight">8dp</item>
        <item name="cornerSizeBottomLeft">8dp</item>
    </style>
</resources>
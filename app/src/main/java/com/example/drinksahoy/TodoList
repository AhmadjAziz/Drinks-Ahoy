- add image for high abv value (important)
- Add image for beers that return null
- Kdoc the project
- put strings like name, tag, abv in strings file.


	BONUS FEATURE
- Work on a recyclerView(important)
- use Ion to download data into the recycler view

Optional Polish
- Data is lost on screen rotation. Can fix with android lifecycles, or maybe be lazy and just check
    check if currentBeer is not null then display it
- Use dokka for documentation instead